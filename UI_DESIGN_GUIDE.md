# 픽셀 아트 UI 디자인 가이드

## 디자인 컨셉

**핵심 아이디어**: 사장님이 회사를 경영하며 부서에 업무를 지시하는 귀여운 픽셀 아트 게임 스타일

**영감 소스**:
- Game Dev Tycoon (게임 개발 시뮬레이션)
- Stardew Valley (픽셀 아트 스타일)
- Papers, Please (사무실 분위기)
- 90년대 경영 시뮬레이션 게임

---

## 컬러 팔레트

### 메인 컬러
```css
/* Primary Colors */
--pixel-blue: #4A90E2;        /* 메인 액션 */
--pixel-green: #7ED321;       /* 성공/완료 */
--pixel-yellow: #F5A623;      /* 경고/진행중 */
--pixel-red: #D0021B;         /* 오류/위험 */
--pixel-purple: #9013FE;      /* 특별/프리미엄 */

/* Background Colors */
--pixel-bg-dark: #2C3E50;     /* 어두운 배경 */
--pixel-bg-medium: #34495E;   /* 중간 배경 */
--pixel-bg-light: #ECF0F1;    /* 밝은 배경 */
--pixel-bg-white: #FFFFFF;    /* 흰색 배경 */

/* Text Colors */
--pixel-text-dark: #2C3E50;   /* 진한 텍스트 */
--pixel-text-medium: #7F8C8D; /* 중간 텍스트 */
--pixel-text-light: #BDC3C7;  /* 밝은 텍스트 */

/* Border & Shadow */
--pixel-border: #34495E;      /* 테두리 */
--pixel-shadow: rgba(0,0,0,0.3); /* 그림자 */
```

### 부서별 컬러
```css
/* Department Colors */
--dept-requirement: #3498DB;  /* 요구사항 - 파랑 */
--dept-planning: #9B59B6;     /* 기획 - 보라 */
--dept-design: #E74C3C;       /* 디자인 - 빨강 */
--dept-frontend: #1ABC9C;     /* 프론트엔드 - 청록 */
--dept-backend: #F39C12;      /* 백엔드 - 주황 */
--dept-qa: #2ECC71;           /* QA - 초록 */
--dept-integration: #95A5A6;  /* 통합 - 회색 */
```

---

## 타이포그래피

### 폰트 패밀리
```css
/* Primary Font - 픽셀 폰트 */
@import url('https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap');

/* Secondary Font - 가독성 좋은 픽셀 폰트 */
@import url('https://fonts.googleapis.com/css2?family=VT323&display=swap');

/* Fallback Font */
font-family: 'Press Start 2P', 'VT323', monospace, sans-serif;
```

### 폰트 크기
```css
/* Heading Sizes */
--font-xxl: 24px;  /* 메인 타이틀 */
--font-xl: 18px;   /* 섹션 헤더 */
--font-lg: 14px;   /* 카드 타이틀 */
--font-md: 12px;   /* 본문 */
--font-sm: 10px;   /* 캡션 */
--font-xs: 8px;    /* 작은 라벨 */

/* Line Height */
--line-height-tight: 1.2;
--line-height-normal: 1.5;
--line-height-loose: 1.8;
```

---

## UI 컴포넌트

### 1. 픽셀 버튼

```css
.pixel-button {
  /* 기본 스타일 */
  padding: 12px 24px;
  border: 4px solid var(--pixel-border);
  background: var(--pixel-blue);
  color: white;
  font-family: 'Press Start 2P', monospace;
  font-size: 12px;
  cursor: pointer;
  position: relative;
  
  /* 픽셀 효과 */
  image-rendering: pixelated;
  image-rendering: -moz-crisp-edges;
  image-rendering: crisp-edges;
  
  /* 그림자 효과 */
  box-shadow: 
    4px 4px 0 var(--pixel-border),
    4px 4px 0 2px var(--pixel-shadow);
  
  /* 트랜지션 */
  transition: all 0.1s ease;
}

.pixel-button:hover {
  transform: translate(-2px, -2px);
  box-shadow: 
    6px 6px 0 var(--pixel-border),
    6px 6px 0 2px var(--pixel-shadow);
}

.pixel-button:active {
  transform: translate(2px, 2px);
  box-shadow: 
    2px 2px 0 var(--pixel-border),
    2px 2px 0 2px var(--pixel-shadow);
}

.pixel-button:disabled {
  opacity: 0.5;
  cursor: not-allowed;
  transform: none;
}
```

**버튼 변형**:
- Primary: 파란색 배경
- Success: 초록색 배경
- Warning: 주황색 배경
- Danger: 빨간색 배경
- Ghost: 투명 배경 + 테두리만

### 2. 픽셀 카드

```css
.pixel-card {
  background: white;
  border: 4px solid var(--pixel-border);
  padding: 16px;
  position: relative;
  
  /* 모서리 효과 */
  clip-path: polygon(
    0 8px, 8px 8px, 8px 0,
    calc(100% - 8px) 0, calc(100% - 8px) 8px, 100% 8px,
    100% calc(100% - 8px), calc(100% - 8px) calc(100% - 8px),
    calc(100% - 8px) 100%, 8px 100%, 8px calc(100% - 8px),
    0 calc(100% - 8px)
  );
  
  box-shadow: 
    8px 8px 0 var(--pixel-border),
    8px 8px 0 2px var(--pixel-shadow);
}

.pixel-card:hover {
  transform: translate(-2px, -2px);
  box-shadow: 
    10px 10px 0 var(--pixel-border),
    10px 10px 0 2px var(--pixel-shadow);
}
```

### 3. 진행률 바

```css
.pixel-progress {
  width: 100%;
  height: 24px;
  background: var(--pixel-bg-light);
  border: 4px solid var(--pixel-border);
  position: relative;
  overflow: hidden;
}

.pixel-progress-fill {
  height: 100%;
  background: var(--pixel-green);
  transition: width 0.3s ease;
  position: relative;
  
  /* 픽셀 패턴 */
  background-image: 
    repeating-linear-gradient(
      90deg,
      transparent,
      transparent 4px,
      rgba(255,255,255,0.2) 4px,
      rgba(255,255,255,0.2) 8px
    );
}

.pixel-progress-text {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  font-family: 'Press Start 2P';
  font-size: 10px;
  color: var(--pixel-text-dark);
  text-shadow: 1px 1px 0 white;
}
```

### 4. 다이얼로그/모달

```css
.pixel-dialog {
  background: white;
  border: 6px solid var(--pixel-border);
  padding: 24px;
  max-width: 600px;
  position: relative;
  
  /* 8비트 스타일 모서리 */
  clip-path: polygon(
    0 12px, 12px 12px, 12px 0,
    calc(100% - 12px) 0, calc(100% - 12px) 12px, 100% 12px,
    100% calc(100% - 12px), calc(100% - 12px) calc(100% - 12px),
    calc(100% - 12px) 100%, 12px 100%, 12px calc(100% - 12px),
    0 calc(100% - 12px)
  );
  
  box-shadow: 
    0 0 0 4px var(--pixel-bg-dark),
    12px 12px 0 4px var(--pixel-shadow);
}

.pixel-dialog-header {
  background: var(--pixel-blue);
  color: white;
  padding: 12px;
  margin: -24px -24px 16px -24px;
  border-bottom: 4px solid var(--pixel-border);
}
```

### 5. 입력 필드

```css
.pixel-input {
  width: 100%;
  padding: 12px;
  border: 4px solid var(--pixel-border);
  background: white;
  font-family: 'VT323', monospace;
  font-size: 16px;
  
  /* 내부 그림자 */
  box-shadow: inset 4px 4px 0 rgba(0,0,0,0.1);
}

.pixel-input:focus {
  outline: none;
  border-color: var(--pixel-blue);
  box-shadow: 
    inset 4px 4px 0 rgba(74, 144, 226, 0.1),
    0 0 0 4px rgba(74, 144, 226, 0.2);
}

.pixel-checkbox {
  width: 24px;
  height: 24px;
  border: 4px solid var(--pixel-border);
  background: white;
  cursor: pointer;
  position: relative;
}

.pixel-checkbox:checked::after {
  content: '✓';
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  font-size: 16px;
  color: var(--pixel-green);
}
```

---

## 캐릭터 디자인

### 사장님 캐릭터 (CEO)
```
크기: 32x32 픽셀
특징:
- 정장 착용
- 안경 착용
- 서류 들고 있음
- 4방향 애니메이션 (정면, 측면, 뒷면)

상태별 애니메이션:
- Idle: 천천히 깜빡임
- Thinking: 턱을 괴고 생각
- Happy: 엄지척
- Worried: 머리를 긁적임
```

### 부서 캐릭터

#### 1. 기획부 (Planning) 💡
```
크기: 24x24 픽셀
컬러: 보라색 계열
특징:
- 노트북 들고 있음
- 안경 착용
- 전구 아이콘 주변에 표시

애니메이션:
- Idle: 노트북 타이핑
- Working: 빠른 타이핑
- Complete: 노트북 닫고 OK 사인
```

#### 2. 디자인부 (Design) 🎨
```
크기: 24x24 픽셀
컬러: 빨간색 계열
특징:
- 팔레트 들고 있음
- 베레모 착용
- 붓 들고 있음

애니메이션:
- Idle: 팔레트 보며 생각
- Working: 붓으로 그리기
- Complete: 작품 들어올리기
```

#### 3. 개발부 (Development) 💻
```
크기: 24x24 픽셀
컬러: 청록색/주황색 계열
특징:
- 후드티 착용
- 헤드폰 착용
- 커피 컵 옆에 배치

애니메이션:
- Idle: 모니터 응시
- Working: 빠른 코딩 (화면 깜빡임)
- Complete: 스트레칭
```

#### 4. QA부 (Quality Assurance) 🔍
```
크기: 24x24 픽셀
컬러: 초록색 계열
특징:
- 돋보기 들고 있음
- 체크리스트 들고 있음
- 진지한 표정

애니메이션:
- Idle: 돋보기로 살펴보기
- Working: 체크리스트 확인
- Complete: 승인 도장 찍기
```

#### 5. 통합부 (Integration) 📦
```
크기: 24x24 픽셀
컬러: 회색 계열
특징:
- 박스 들고 있음
- 작업복 착용
- 헬멧 착용

애니메이션:
- Idle: 박스 정리
- Working: 박스 쌓기
- Complete: 완성된 제품 들어올리기
```

---

## 화면별 레이아웃

### 1. 메인 대시보드 (사장실)

```
┌─────────────────────────────────────────────────────────────┐
│  🏢 My Dev Company              [💰 Credits: 1000] [👔 CEO]  │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  📋 사장님의 책상                                       │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐            │ │
│  │  │ 📝 새     │  │ 🔄 진행중 │  │ ✅ 완료   │            │ │
│  │  │ 프로젝트  │  │    (3)    │  │   (12)   │            │ │
│  │  └──────────┘  └──────────┘  └──────────┘            │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  🏭 우리 회사 부서들                                    │ │
│  │                                                         │ │
│  │  ┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐│ │
│  │  │ 💡  │  │ 🎨  │  │ 💻  │  │ 💻  │  │ 🔍  │  │ 📦  ││ │
│  │  │기획부│  │디자인│  │개발부│  │개발부│  │ QA  │  │통합부││ │
│  │  │     │  │     │  │(FE) │  │(BE) │  │     │  │     ││ │
│  │  │대기중│  │대기중│  │대기중│  │대기중│  │대기중│  │대기중││ │
│  │  └─────┘  └─────┘  └─────┘  └─────┘  └─────┘  └─────┘│ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  📊 최근 프로젝트                                       │ │
│  │  ┌──────────────────────────────────────────────────┐ │ │
│  │  │ ✅ Todo App                    2024-02-05  [보기] │ │ │
│  │  ├──────────────────────────────────────────────────┤ │ │
│  │  │ 🔄 E-commerce Site (75%)       2024-02-06  [보기] │ │ │
│  │  ├──────────────────────────────────────────────────┤ │ │
│  │  │ 🔄 Blog Platform (30%)         2024-02-07  [보기] │ │ │
│  │  └──────────────────────────────────────────────────┘ │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 2. 프로젝트 생성 설문

```
┌─────────────────────────────────────────────────────────────┐
│  📝 새 프로젝트 만들기                          [1/5 단계]   │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  [████████░░░░░░░░░░░░░░░░░░░░░░] 20%                 │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  프로젝트 기본 정보                                     │ │
│  │                                                         │ │
│  │  프로젝트 이름 *                                        │ │
│  │  ┌─────────────────────────────────────────────────┐  │ │
│  │  │ My Awesome App                                   │  │ │
│  │  └─────────────────────────────────────────────────┘  │ │
│  │                                                         │ │
│  │  서비스 설명 *                                          │ │
│  │  ┌─────────────────────────────────────────────────┐  │ │
│  │  │ 사용자가 할 일을 관리할 수 있는 앱                │  │ │
│  │  └─────────────────────────────────────────────────┘  │ │
│  │                                                         │ │
│  │  프로젝트 타입 *                                        │ │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐              │ │
│  │  │ ☑ 웹    │  │ ☐ 모바일│  │ ☐ 데스크│              │ │
│  │  │   앱    │  │   앱    │  │   톱    │              │ │
│  │  └─────────┘  └─────────┘  └─────────┘              │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  [◀ 이전]                                      [다음 ▶]     │
└─────────────────────────────────────────────────────────────┘
```

### 3. 프로젝트 진행 대시보드

```
┌─────────────────────────────────────────────────────────────┐
│  🚀 Todo App 개발 중...                    [⏸️ 일시정지]    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  전체 진행률: 65%                                       │ │
│  │  [████████████████████░░░░░░░░░░░░░░]                 │ │
│  │  완료: 4/7 부서  |  예상 완료: 약 5분 남음              │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ 💡 기획부    │  │ 🎨 디자인부  │  │ 💻 개발부(FE)│     │
│  │ ✅ 완료      │  │ ✅ 완료      │  │ 🔄 작업중    │     │
│  │ ████████████ │  │ ████████████ │  │ ████████░░░░ │     │
│  │ 100%         │  │ 100%         │  │ 75%          │     │
│  │              │  │              │  │              │     │
│  │ 💬 "기능 명세│  │ 💬 "UI 구조  │  │ 💬 "컴포넌트 │     │
│  │ 작성 완료"   │  │ 설계 완료"   │  │ 구현 중..."  │     │
│  │              │  │              │  │              │     │
│  │ 📄 산출물:   │  │ 📄 산출물:   │  │ 📄 산출물:   │     │
│  │ • 기능명세.md│  │ • UI설계.md  │  │ • App.tsx    │     │
│  │ • 플로우.md  │  │ • 컴포넌트.md│  │ • Header.tsx │     │
│  │ [상세보기]   │  │ [상세보기]   │  │ [상세보기]   │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  📋 실시간 로그                                         │ │
│  │  ┌──────────────────────────────────────────────────┐ │ │
│  │  │ [14:32:45] ✅ 기획부: 기능 명세서 생성 완료       │ │ │
│  │  │ [14:32:46] 🔄 디자인부: UI 구조 설계 시작        │ │ │
│  │  │ [14:33:12] 🔄 디자인부: 컴포넌트 분해 중...      │ │ │
│  │  │ [14:33:45] ✅ 디자인부: UI 설계 완료             │ │ │
│  │  │ [14:33:46] 🔄 개발부(FE): 프로젝트 초기화        │ │ │
│  │  │ [14:34:12] 🔄 개발부(FE): 컴포넌트 생성 중...    │ │ │
│  │  └──────────────────────────────────────────────────┘ │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

---

## 애니메이션

### 1. 로딩 애니메이션

```css
@keyframes pixel-spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.pixel-loader {
  width: 32px;
  height: 32px;
  border: 4px solid var(--pixel-bg-light);
  border-top: 4px solid var(--pixel-blue);
  animation: pixel-spin 1s steps(8) infinite;
}
```

### 2. 타이핑 효과

```css
@keyframes typing {
  from { width: 0; }
  to { width: 100%; }
}

@keyframes blink {
  50% { border-color: transparent; }
}

.typewriter {
  overflow: hidden;
  border-right: 4px solid var(--pixel-text-dark);
  white-space: nowrap;
  animation: 
    typing 3s steps(40) 1s forwards,
    blink 0.75s step-end infinite;
}
```

### 3. 깜빡임 효과

```css
@keyframes pixel-blink {
  0%, 49% { opacity: 1; }
  50%, 100% { opacity: 0; }
}

.pixel-blink {
  animation: pixel-blink 1s steps(1) infinite;
}
```

### 4. 흔들림 효과 (오류 시)

```css
@keyframes pixel-shake {
  0%, 100% { transform: translateX(0); }
  10%, 30%, 50%, 70%, 90% { transform: translateX(-4px); }
  20%, 40%, 60%, 80% { transform: translateX(4px); }
}

.pixel-shake {
  animation: pixel-shake 0.5s ease-in-out;
}
```

### 5. 바운스 효과 (완료 시)

```css
@keyframes pixel-bounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-8px); }
}

.pixel-bounce {
  animation: pixel-bounce 0.6s ease-in-out;
}
```

---

## 반응형 디자인

### 브레이크포인트
```css
/* Mobile */
@media (max-width: 640px) {
  --font-xxl: 18px;
  --font-xl: 14px;
  --font-lg: 12px;
  --font-md: 10px;
}

/* Tablet */
@media (min-width: 641px) and (max-width: 1024px) {
  --font-xxl: 20px;
  --font-xl: 16px;
  --font-lg: 13px;
  --font-md: 11px;
}

/* Desktop */
@media (min-width: 1025px) {
  --font-xxl: 24px;
  --font-xl: 18px;
  --font-lg: 14px;
  --font-md: 12px;
}
```

---

## 접근성 (Accessibility)

### 키보드 네비게이션
```css
.pixel-button:focus,
.pixel-input:focus {
  outline: 4px dashed var(--pixel-blue);
  outline-offset: 4px;
}
```

### 고대비 모드
```css
@media (prefers-contrast: high) {
  :root {
    --pixel-border: #000000;
    --pixel-text-dark: #000000;
    --pixel-bg-light: #FFFFFF;
  }
}
```

### 다크 모드
```css
@media (prefers-color-scheme: dark) {
  :root {
    --pixel-bg-dark: #1A1A1A;
    --pixel-bg-medium: #2D2D2D;
    --pixel-bg-light: #3A3A3A;
    --pixel-bg-white: #2D2D2D;
    --pixel-text-dark: #FFFFFF;
    --pixel-border: #4A4A4A;
  }
}
```

---

## 사운드 효과 (선택사항)

### 효과음 목록
```javascript
const sounds = {
  buttonClick: 'sfx/button-click.wav',
  taskComplete: 'sfx/task-complete.wav',
  error: 'sfx/error.wav',
  notification: 'sfx/notification.wav',
  typing: 'sfx/typing.wav',
  success: 'sfx/success.wav',
};

// 사용 예시
function playSound(soundName) {
  const audio = new Audio(sounds[soundName]);
  audio.volume = 0.3;
  audio.play();
}
```

---

## 구현 체크리스트

### Phase 1: 기본 스타일
- [ ] CSS 변수 설정
- [ ] 픽셀 폰트 임포트
- [ ] 기본 리셋 스타일
- [ ] 컬러 팔레트 적용

### Phase 2: 컴포넌트
- [ ] 픽셀 버튼
- [ ] 픽셀 카드
- [ ] 진행률 바
- [ ] 입력 필드
- [ ] 다이얼로그

### Phase 3: 캐릭터
- [ ] 사장님 캐릭터 스프라이트
- [ ] 부서별 캐릭터 스프라이트
- [ ] 애니메이션 프레임
- [ ] 상태별 애니메이션

### Phase 4: 레이아웃
- [ ] 메인 대시보드
- [ ] 설문 페이지
- [ ] 프로젝트 진행 페이지
- [ ] 결과물 페이지

### Phase 5: 애니메이션
- [ ] 로딩 애니메이션
- [ ] 트랜지션 효과
- [ ] 호버 효과
- [ ] 상태 변화 애니메이션

### Phase 6: 반응형
- [ ] 모바일 레이아웃
- [ ] 태블릿 레이아웃
- [ ] 데스크톱 레이아웃
- [ ] 터치 인터랙션

이 디자인 가이드를 따라 구현하면 일관성 있고 매력적인 픽셀 아트 UI를 만들 수 있습니다.